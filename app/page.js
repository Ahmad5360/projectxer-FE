"use client";

import HomePage from "./homePage";

const Home = () => {
  
  return (
    <div>
      <HomePage />
    </div>
  );
  // if (typeof window !== "undefined") {
  //   return <LoginPage/>
  // } else {
  //   return <Loader />;
  // }
};

export default Home;
